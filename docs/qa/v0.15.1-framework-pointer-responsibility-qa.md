# v0.15.1 画布 pointer responsibility QA

日期：2026-07-29

## 修订合同

- 只有 GraphCanvas 背景与非交互层可开启 pan session 并取得 pointer capture。
- `.graph-node`、按钮、链接和表单控件的 `pointerdown` 不再冒泡进入画布拖拽责任；节点本身继续处理 click、Enter 与 Space。
- 背景实际拖动后只抑制该拖动手势随后的背景 click；下一次节点 click 不受污染。

## 自动检查

- `tests/framework-experience.test.mjs` 锁定节点/工具控件拒绝进入 canvas pointer session、背景仍保留 pointer capture、拖动抑制与下钻 activate 的责任边界。
- `npm run release:check` 通过：49/49 tests。

## 本地浏览器回归

- 390×844：以真实坐标鼠标点击总览“数字化实现”，URL 进入 `?view=digital-implementation`，页面与 body 均无横向溢出。
- 同一总览节点：Playwright click、Enter、Space 均进入局部；普通“企业运作”节点仍只选中解释、URL 保持总览。
- 320/1440：继承 v0.15.0 已验收的 desktop/mobile world、无横向溢出、返回/复位与焦点恢复合同；本次改动只收窄 pointer session 的事件入口，不影响投影与几何。
