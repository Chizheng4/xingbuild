# v0.15.0 企业经营体系局部视图 QA

日期：2026-07-29

## 自动合同

- `digital-implementation` 保持 9 节点、13 边和默认 `b2b-product-architecture`；总览与局部的 desktop/mobile 几何均通过节点、端点和无关节点穿越检查。
- URL 只接受 `?view=digital-implementation`；未知值回退总览。进入使用 push，显式返回使用 replace 并恢复总览节点焦点。
- 局部的说明、直接关系、选中态、复位和 aria-live 均由当前 architecture 驱动。

## 本地浏览器

- 1440：局部 SystemStage 为 722×451（16:10），9 节点、13 边、1 条无箭头反馈轨；无横向溢出。
- 1024 / 768 / 557：继续使用完整 desktop projection，画布分别为 715×446、687×429、476×297；无横向溢出。
- 390：局部 mobile world 为 333×961，固定视窗为 333×518；默认同时可见企业业务架构、B端产品架构与数据架构，向下可达反馈链，说明与画布间距 14px。
- 320：固定视窗为 271×458；无横向溢出。
- 交互：click/Enter 均可进入局部；局部复位恢复 B端产品架构与原点；显式返回恢复总览“数字化实现”的选中与焦点；Back、直接 URL 与未知 view 均已检查。
- console error/warning：0。
