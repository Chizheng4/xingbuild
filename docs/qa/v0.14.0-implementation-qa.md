# v0.14.0 实现与发布前 QA

## 结构合同

- 首页的唯一 H1 位于完整 shell 定位行，后续 TwoColumnLayout 才开始 Robotaxi 与企业经营体系投影；Rail 仅由该内容层渲染一次。
- `PracticePresentation` 以传入根标题等级递进模块标题；`BusinessObservationPresentation` 是首页与经营观察页共享的同源投影。
- Framework 的移动端说明紧随 SystemStage，显示“当前节点”状态，但定义、作用和直接关系仍只读取 `frameworkModel`。

## 视觉合同

- Header 在滚动超过 8px 后以全视口暖白透明层、12px blur、轻影表达悬浮阅读层，不改变 Header 尺寸或 wordmark 颜色。
- 排版只使用 positioning、wordmark、page/object、article、section、reading、summary、meta 角色 token；移动 Stage→解释为 12–16px，不同对象保持 40–48px。

## 本地验证记录

- 自动：`npm run release:check` 通过（47/47），`git diff --check` 通过。
- 1440×1000：首页 H1 为 32px / 46.4px；滚动后 Header 仍为 56px，settled `backdrop-filter` 为 `blur(12px) saturate(.96)`；双栏为 952px / 24px / 304px，SystemStage 为 724px。
- 1024×768：Framework Canvas 为 717×448，键盘选择“企业运作”后 `aria-pressed` 与说明同步；无横向溢出。
- 768×1024：集中观察 ReadingShell 为 689px、8 个同源 ObservationBlock；无横向溢出。
- 557×816：legacy Article 只有一个 reader-facing H1，未出现治理 Heading 或目录；无横向溢出。
- 390×844：Framework Canvas 为 335×300，Stage→当前节点状态面为 14px；移动菜单固定覆盖、背景滚动锁定，Escape 可恢复；无横向溢出。
- 320×568：首页 H1 为 24px / 34.8px，Header 为 52px、身份 lockup 保持 `nowrap`，Stage→模块解释为 14px；无横向溢出。
- 已保存当前有效视觉证据：`v0140-home-scrolled-1440.png`、`v0140-framework-selected-390.png`、`v0140-home-320.png`。浏览器控制台 error/warn 为 0。

不把八条 Brief 或 approved media 的事实内容改写为 QA 内容。
