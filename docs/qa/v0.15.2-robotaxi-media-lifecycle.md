# v0.15.2 Robotaxi 媒体生命周期 QA

## 上游同步事实

- Robotaxi 权威快照：`v049.13.15` / `1e01d4998f21212f4c716522fbb1f880fbee73b8`。
- 当前 publication 为 `suspended`；manifest 为 `superseded` / `internal`。
- 城市空间媒体为 `revoked` / `internal`；其余三项为 `pending_review` / `paused` / `internal`。
- 四项原始媒体、SHA-256、版本、commit、历史 approval 与本次 review 记录均保留在内部 archive；没有当前公开媒体。

## 公开投影合同

- 只有 publication active、manifest/资产 review approved、资产 approval approved、资产 publicStatus public，且文件、哈希与 provenance 校验成功时，Practice 才投影模块。
- 当前 `findPractice("robotaxi").modules` 为零；模块内容关系仍保留在受控 Practice 内容中，媒体重新逐项批准后可只通过内容更新恢复。
- 首页和 `/products` 因此不显示历史媒体、残缺模块或空媒体框。

## 自动验证

- `npm run practice:check`：四项历史媒体均可在 archive 中按 SHA-256 核验，公开模块数为零。
- 状态矩阵覆盖 active/public/approved 唯一放行路径，以及 suspended、superseded、internal、revoked、pending_review、paused、哈希不合法的拒绝路径。
- content-only scope 与发布脚本覆盖单一媒体 manifest 加必要 archive/public 资产，并在发布前执行 Practice 校验。

## 浏览器最小回归

- 以无截图、单页串行方式检查首页与 `/products` 的 Robotaxi 投影：无 Practice 模块、无媒体框、无横向溢出，其他版式不变。
