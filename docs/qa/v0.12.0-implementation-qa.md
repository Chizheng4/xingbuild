# v0.12.0 实现 QA

## 范围

- Header 单行身份组、首页与 Robotaxi 独立路由、作品母版与观察的显式 reader presentation。
- 不包含生产发布、DNS 或 Robotaxi 模块媒体填充。

## 自动检查

- `npm run release:check`：通过，52 项测试。
- `git diff --check`：通过。
- `content:check`：10 条已发布 ObservationPublication；8 条 scheduled 内容均为显式 Brief。

## 本地浏览器

- 1440×1000：Header 72px；identity lockup 为单行、8px gap；双栏主栏 848px + 48px gap + 320px rail；观察集合 736px、left 345px；无横向溢出。
- 1024×768：双栏按实际可容纳条件转单列，观察集合仍为 736px；无横向溢出。
- 390×844：Header 64px，lockup 宽 164px 且不换行；菜单为固定全视口并锁定背景滚动；观察流 8 条、无卡片/标题、存在来源行；框架画布 335×300，说明位于 rail 前。
- 320×568：lockup 保留署名且不换行，根页与遗留文章路径均无横向溢出；Brief 旧路径安全归一化至 `/observations`。
- Framework：1440 画布 848×530 (16:10)、7 个节点；点击“企业运作”同步解释；拖拽得到平移 `(68,42)`，复位恢复 `(0,0)`。

## 已知内容边界

- Robotaxi 仍无已批准模块与媒体，因此页面只呈现真实作品说明与边界，不生成占位截图、模块或泛链接。
- 两篇既有文章保留兼容路径；它们不进入新的 Brief 流。后续 Article 仅通过显式 `presentation: "article"` 与 `article.sourceRefs` 增加。

## 专业复验修订

- Brief 固定为四个相邻阅读角色：`identity`（事件日期 · 主体）→ `dimension`（`#维度`，必要时 `#观点`）→ statement → 来源链接。1440px 的集合间距为 20px，390px 为 16px；八条 Brief 的上下边框均为 0px，维度行不含分隔点。
- 遗留文章 `/observations/four-planes-of-enterprise-digitalization` 在 390px 仅保留一个标题、维度、摘要、正文与最后一行内联来源链接；目录、治理标题、事实概览、来源列表和证据边界 reader UI 均未渲染。
- 短内容页不再由 `main` 的 flex 增长撑满视口。1440px：根页主内容底部 528px、footer 顶部 584px；Robotaxi 主内容底部 443px、footer 顶部 499px，均为 56px 正常关系间距而非贴视口底部。390px：根页主内容 64–1085px、footer 顶部 1125px；Robotaxi 主内容 64–534px、footer 顶部 574px，均为 40px 正常关系间距。
- 修订后本地 Chrome console error/warning 均为 0，1440px 与 390px 复验页面无横向溢出。
