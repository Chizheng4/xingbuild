# v0.14.1 Framework 标题层级 QA

## 固定合同

- 企业经营体系来源页：`H1 企业经营体系 → H2 当前节点 → H3 定义 / 作用 / 直接关系`。
- 首页同源投影：`H2 企业经营体系 → H3 当前节点 → H4 定义 / 作用 / 直接关系`。
- 选中节点与内部说明的视觉角色分开选择器维护，因此语义递进不会改变字号、行高、颜色或相邻间距。

## 本地验证记录

- 自动：`node --test tests/framework-experience.test.mjs` 8/8、`npm run release:check` 48/48、`git diff --check` 通过。
- 1440 本地首页：企业经营体系投影实际为 `H2 → H3 → H4`；三项内部说明均为既有 `12px / 18.6px` 辅助标题视觉；控制台 error/warn 为 0。
- 1440 本地来源页：实际为 `H1 → H2 → H3`；内部说明视觉保持同一既有角色；控制台 error/warn 为 0。
- 390 本地首页与来源页：标题层级分别保持 `H2 → H3 → H4` 和 `H1 → H2 → H3`；未发现可见元素越过内容视口边界。

不改变 FrameworkModel、概念、边集、箭头、文案、交互或页面视觉结构。
