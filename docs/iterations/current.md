# 当前迭代

## 当前稳定版本

`v0.7.1`

## 目标版本

暂无，等待下一轮迭代定义。

## 本轮完成

- 关闭 Vite 对启动终端普通字符的读取；
- 防止误触 `h`、`r` 或 `q` 触发帮助、重启或退出；
- 保留固定端口、自动打开预览、本地/线上链接和 `Control-C` 停止能力；
- 完成实际进程启动/停止验证和完整发布前检查。

完成记录：

- `docs/iterations/history/v0.7.1.md`

## 发布状态

- GitHub：未推送；
- EdgeOne：未发布；
- 域名：未修改。
