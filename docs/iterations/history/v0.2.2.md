# v0.2.2 响应式品牌一致性

## 问题

页面缩小到 `900px` 后，响应式规则把暖白、墨色和赭色改成白色、深黑和蓝色，使同一网站在缩放时像切换了另一套品牌。

## 根因

布局断点错误承担了品牌主题切换责任，响应式布局与视觉身份发生耦合。

## 完成结果

- 全部视口统一使用暖白、墨色、线条灰和赭色品牌令牌。
- 手机继续保留无衬线层级、紧凑导航、单列布局和纵向架构图。
- 增加自动检查，禁止在响应式断点中重新定义全局品牌颜色。
- 在 `901px`、`900px` 和 `390px` 真实页面中验证计算颜色完全一致。
