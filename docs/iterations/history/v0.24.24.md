# v0.24.24 — 内容源与不可变产品基座解耦

- 版本：`v0.24.24`
- commit：`a6d03a4`（Engineering 本地实现提交）
- annotatedTag：`v0.24.24`
- clean：`true`（history 收口提交与 tag/preflight 后）
- parent：`v0.24.23` / `0bbff29fc7fe10e3d617eb76115b7ae0c5aa4b09`

## 范围

- `baseSiteArtifact` 携带实际 source bundle、sourceDirectory 与 sourceBundleHash；内容 staging 只从显式不可变基座叠加独立内容快照，不复制当前 canonical sourceRoot，也不从 dist 隐式推断基座。
- 日常 Observation、Article、Practice、B 端内容、媒体与发布输入迁移到 ignored `.content-workspace/content`；产品 Git 保留能力合同、schema 与稳定 registry 模板，不枚举当前 asset/module ID。
- 字段级 ChangeSet/recovery 支持稳定模板解析、新 image/video asset、模块绑定和清空 `mediaId`，保留 beforeHash、来源、审批、路径与固定目标门禁。

## 不做

- 不修改 UI、IA、schema、路由、页面结构、视觉系统或上游 Robotaxi 事实；不引入 CMS/RBAC、外部数据层或第二套发布引擎。
- 不回写、移动或覆盖 v0.24.23 tag；不创建 branch、worktree、task、automation/scheduler；不 push、publish、deploy。

## 验收合同与证据

- `npm run check`、`npm run release:prepare`、`git diff --check` 与内容/registry/Practice 专项通过；完整 Sites 测试 `141` 项中 `140` 通过。
- 唯一失败为既有 Mermaid/Puppeteer macOS 浏览器 I/O（`tests/framework-experience.test.mjs`），未修改该环境问题；release build 已生成并校验 Sites 产物与 source bundle。
- `npm run release:closeout-check` 在 history 收口前通过；history 提交后创建 annotated `v0.24.24` 并运行 `npm run release:preflight`。
- Local URL：`http://127.0.0.1:4317/`（本次未启动）。Online：`https://xingbuild.top/` 继续既有线上版本，未执行本版本 publish。
- 活动 candidates 无新增；内容 task 仅完成独立输入准备，待产品/视觉验收后按 immutable baseSiteArtifact 独立发布。
