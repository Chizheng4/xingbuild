# v0.25.8 — ContentReleaseReceipt 投影一致性与 active 快照

- version: `v0.25.8`
- parent: `v0.25.7 / 5a983e3aca7ce7cb1cab153b50ee0789d698ea76`
- scope: use finalized `ContentReleaseReceipt` facts as the single-release source of truth; normalize all receipts and candidates into one `ActiveContentSet`; generate global snapshot collections only from that set; keep package projection checks limited to package identity.
- acceptance: Didi finalized receipt remains active despite a package projection missing global collections; Ojai `revision-7e65a94afb3333fa` can replace its logical slot without dropping active content; Waymo service `revision-d7506767085cfd37` remains prepared; repeated assembly keeps one publication identity and no duplicate deployment; projection identity drift and local identity omissions hard fail.
- contentBoundary: content body, source, review, publishedAt, media facts, hashes and existing contentReleaseId/packageRevisionId are unchanged; no direct content transport is executed by Engineering implementation.
- commit: recorded by Engineering closeout.
- annotatedTag: `v0.25.8` after final closeout commit.
- clean: required before product/visual acceptance.
- publish: not executed before product/visual acceptance.
