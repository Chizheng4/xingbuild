# v0.15.6 — Robotaxi 页面 canonical 收口

## 目标

收口 Robotaxi 页面 canonical，使 `/products` 成为唯一真实读者页面，并让历史详情地址以 replace 方式兼容跳转。

## 完成范围

- `/products` 保持唯一真实页面与 canonical reader URL。
- `/products/robotaxi` 使用 replace 跳转至 `/products`，不增加浏览器历史层。
- `/robotaxi`、`/works`、`/works/robotaxi` 的既有兼容结果保持为 `/products`。
- 删除 `/products/robotaxi` 独立页面与标题分支；未修改 RobotaxiPage、内容、视觉、Footer、观察、外部 action 或发布脚本。

## 验证与发布

- canonical 路由专项测试与完整 `npm run release:check` 63/63 通过。
- 1440×900 与 390×844 的直接进入、最终 URL、标题、当前导航、replace 历史、横向溢出和 console 验证通过。
- 产品独立差异复核通过；本地 commit/tag、GitHub push、EdgeOne 部署及公网 release/manifest 同 SHA 验收完成。

## 归档状态

v0.15.6 已完成实现、验证、专业验收、本地收口与公网发布；后续迭代不得回写本历史。
