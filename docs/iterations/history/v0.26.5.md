# v0.26.5 — 页面独立内容接入与兼容重构

父版本：`v0.26.4` / `d5795b64a2a65bd13fe755e1ae4cc199c9384969`

## 范围

- 首页与 `/products` 各自拥有页面内容组合、CTA、媒体槽位、空状态和 ClosingAction；两页仍可读取同一 Robotaxi 内容对象。
- 将共享层收敛为内容对象读取、基础展示 primitives、MediaStage、ActionGroup、tokens、媒体安全链接和 empty fallback；不再由 `ShowcaseFlow → PracticePresentation` 持有页面级编排开关。
- `pageDefinitions` 去除 `shared-content-projection` / “统一投影”语义，保留现有路由、ContentSet、内容身份与 Site Publication 入口。

## 验证合同

- 页面投影独立性、同对象不同结构、空内容 fallback 与兼容入口回归。
- Web `1600×1067`、Mobile `390×844`、窄屏 `320×844`、等效 200% 缩放；五路由每页一个 H1、无横向溢出、无 console/page error。
- `npm run check`、`npm run release:prepare`、分层 QA、`npm run release:closeout-check`、`npm run release:preflight`、`git diff --check` 与提交后 exact HEAD `release:build`。

## 状态

Engineering local implementation checkpoint；待产品/视觉独立验收，尚未 product transport。
