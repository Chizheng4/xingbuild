# v0.25.5 — 内容发布站点快照身份与可恢复发布

- version: `v0.25.5`
- parent: `v0.25.4 / 99dcd94b08f8b2353632ce8a33c6dd12928dfddf`
- scope: make `ContentReleaseReceipt` the active lifecycle source; atomically project all active receipts and the candidate into one SitePublication; bind snapshot identity, global lease, deployment resume, full public verification and recovery-safe finalize.
- acceptance: active IDs and typed target collections correspond exactly; legacy projections cannot silently drop receipts; three retained Briefs and the earlier NHTSA receipt assemble with all existing active content; partial/drifting manifests hard fail; resume reuses the same deployment; product build remains content-isolated; full project/release checks pass.
- contentBoundary: content body, source, review, media facts, publishedAt and existing contentReleaseId are unchanged; no content release or direct EdgeOne transport is executed by Engineering implementation.
- commit: recorded by Engineering closeout.
- annotatedTag: `v0.25.5` after final closeout commit.
- clean: required before product/visual acceptance.
- publish: not executed before product/visual acceptance.
