# v0.24.21 — 内容目标入口与可验证恢复包

- version: `v0.24.21`
- commit: `fb6038dcc70cf1d4b546b13646b8eecac445d6e5`（Engineering 本地实现提交）
- annotatedTag: `v0.24.21`
- clean: `true`（本 history 收口提交后）
- parent: `v0.24.20` / `c87df93ff89caee0e53f04d6013f72306fbb0900`

## 范围

- 增加非发布 `content:target` 入口：从 registry 输出唯一定位卡，并按明确的 `after`、`sourceRefs`、`boundary` 与 `authority` 生成 ignored ChangeSet。
- ChangeSet 保留原始 `before`/`after` 与 hash；prepare 后关联 `contentReleaseId` 和 ignored release package，并能生成带原始发布身份的逆向 rollback ChangeSet。
- 逆向 ChangeSet 通过既有内容 `prepare → build → transport` 形成可验证恢复包；canonical 内容、产品版本身份与产品 manifest 不变。
- registry 增加 target、scope、type、sourcePath、fieldPath、route 与路径边界完整性门禁。

## 不做

- 不修改 v0.24.20 tag；不改 UI、IA、schema、页面结构、路由、组件、CSS、交互、上游事实或 Practice video/媒体合同。
- 不扩大 Article、About、Observation 或首页字段白名单；不新增第二套内容发布引擎、CMS、RBAC、task、branch、worktree 或 automation。
- 不 push、publish、部署或公网验收。

## 验收合同

- `npm run check`、`npm run release:prepare`、`npm run release:closeout-check`、`git diff --check` 通过。
- 内容目标、registry integrity/path boundary、ChangeSet、staging overlay、recovery package 与产品 transport 隔离专项共 26/26 通过。
- Local URL `http://127.0.0.1:4317/` 未启动；Online 仍为 `https://xingbuild.top/` 的既有线上版本；活动 candidates 无新增。
