# v0.24.29

- version: `v0.24.29`
- implementation commit: `3c3949f`
- annotatedTag: `v0.24.29` (created after history closeout)
- parent: `v0.24.28` / `0a8fc2a90764ddd6ea0e61175d50b5fd4918b290`
- scope: deterministic product release gates, environment QA separation, and compatible immutable content base artifacts
- clean: required and verified by final preflight

## Acceptance

`release:build` now produces deterministic identity-matched artifacts; `release:qa` retains Mermaid/Puppeteer and other environment/browser QA as an independently recorded gate. Product transport still requires clean HEAD/tag, matching manifests, fixed EdgeOne target, and public identity verification. Content base artifacts are explicitly validated for immutable source and optional capability compatibility, allowing the registered v0.24.26 base without waiting for a newer product release.

## Verification

- `npm run check`: passed
- `npm run release:prepare`: passed
- `npm run release:build`: passed
- `npm run release:closeout-check`: passed before history closeout
- `git diff --check`: passed
- `npm run release:preflight`: run after annotated tag
- `release:qa`: retained separately; existing Mermaid/Puppeteer macOS I/O incident is recorded and does not remove QA

## Release status

Local only; no push/publish/deploy. Local URL `http://127.0.0.1:4317/` not started. Online remains v0.24.26 at `https://xingbuild.top/` pending product/visual acceptance and authorized transport.
