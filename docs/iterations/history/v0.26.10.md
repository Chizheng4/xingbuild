# v0.26.10 — 全站垂直节奏与 ProductHero 密度

父版本：`v0.26.9` / `13119436d2a6f0a07f2a3316c3a81c23efd28c4c`

## 实现

- 新增首页 Focus、定位→CTA、CTA→最新作品和经营观察页眉语义节奏 token；Home/Product 页面 flow 不再叠加旧 page gap 与补偿 margin。
- 将 ShowcaseModule→ClosingAction 统一为 Web `96px` / Mobile `56px`，与模块列表节奏一致；Home 与 Products 投影继续独立。
- 将 LatestUpdateCard 收口为 `40px` 高、`8px` 上下 padding、`13px` 文字，并将卡片→ProductHero 调整为 `24px`。
- 经营观察 H1→首内容行收口为 `64/48px`，两栏首内容起点为 `16px`；未改变内容事实或投影。

## 验证

- viewport-only `20/20` 截图尺寸精确为 `1600×1067`、`1280×1067`、`390×844`、`320×844`；五路由 `overflow=false`、`main=1`、`h1=1`、console/page error 为 0。
- Home `64/40`、`40/24`、`64/40`、`4/4`；Business `64/48`、`16/16`；ClosingAction `96/56`；LatestUpdateCard `40/8/13` 与 ProductHero 三组关系通过。
- axe `0` violations、键盘 focus、Reduced Motion、四视频行为和安全链接通过；既有 content fixture/environment failures 分层保留。

## 边界

未修改 ContentSet、正文、审核、来源、媒体、content CLI、Coordinator、ProductArtifact/SiteSnapshot/SitePublication 语义；未运行 product transport 或 content publish。
