# v0.24.14 发布 transport 目标与故障决策门治理

## 结果

- 目标：将 prepare、build、closeout/preflight 与 transport-only publish 固定为四个阶段，消除旧规则中 publish 构建沙箱、release:check、Sites 与强制 worktree 的冲突。
- 范围：publish 只消费 clean direct-local `main` 的既有 HEAD/tag 与预生成 `dist/client`；固定 EdgeOne `xingbuild-nochina` / `makers-ze0f6txvlhco` / `xingbuild.top`；新增 Publish Incident 失败停止与路由字段。
- 不做：不修改 UI、IA、schema、内容或上游事实；不回写 v0.24.13，不移动既有 tag；本版本不 push/publish/deploy。

## 验收标准

- 规则、AGENTS、README 与脚本边界一致；transport 不调用 release:check/build/generators/业务 QA。
- 固定 EdgeOne 目标与 `dist/client` manifest 身份不匹配时硬失败；失败记录阶段化事实并停止后续动作。
- `check`、专项 publish/rules 测试、`release:prepare`、`release:build`、closeout、preflight、diff-check 记录实际结果。

## 状态

- localSubmission: complete（Engineering 本地 commit/tag 收口时冻结）。
- 本地 URL：http://127.0.0.1:4317/（未启动）；线上继续 v0.24.13：https://xingbuild.top/。
- 活动候选无新增，仅保留未确认 DRAFT/pending。
