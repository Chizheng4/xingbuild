# v0.25.14 — 内容首次发布与修订发布时间分层

- version: `v0.25.14`
- parent: `v0.25.13 / e63ff943d4d17b2a8fdf61c4bd59dd070d9904e3`
- scope: separate logical `firstPublishedAt` from per-revision `revisionReleasedAt`; retain legacy `publishedAt` as a first-publication projection and make replacement candidate null times recoverable.
- acceptance: pending independent product/visual review after Engineering local closeout; lifecycle resolver, replacement/reconcile, finalize, public projection, resume and regression evidence required.
- contentBoundary: preserve v0.25.13 visual behavior, four Robotaxi media slots, 34 active content identities, bodies, approvals, media provenance and hashes; content task may resume only after product acceptance and product online verification.
- commit: recorded by Engineering closeout.
- annotatedTag: `v0.25.14` after final closeout commit.
- clean: required before product/visual acceptance.
- publish: not executed before product/visual acceptance.

## Engineering implementation

- 新增统一 content lifecycle time resolver：首次公开时间只属于 logical content，revision 发布时间只在本 revision finalize 时生成；新 logical content 首次 finalize 使用同一时间，replacement 继承 active first publication。
- receipt、completion、package projection 与整站 SitePublication 公网投影校验统一使用该 resolver；旧 `publishedAt` 仅作兼容读取，不迁移或回写历史 receipt。
- replacement/reconcile 允许 candidate `revisionReleasedAt=null`，对显式不同 `firstPublishedAt` 硬失败；resume 保持原 package、ChangeSet、contentHash、publication/deployment 身份。
- 本地 Engineering 版本不执行内容 transport 或产品 publish，待产品/视觉双门禁验收。
