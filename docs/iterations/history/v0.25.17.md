# v0.25.17 — 不可变 Revision 与 Registry Lineage Binding

- version: `v0.25.17`
- parent: `v0.25.16 / 20b6c5fb49b007b7655c6c4f113af81b5bb5dfdc`
- scope: persist an immutable `PublicationLineageBinding` derived from `ContentSlotRegistry`; use it across SitePublication, Coordinator finalize, and receipt/completion projections without rewriting legacy revisions.
- compatibility: legacy self-reference `practice-robotaxi-604214b3bfddf09f` / `revision-9bb22df0f30845e8` resolves to active predecessor `practice-robotaxi-d67fcedd760acc5a`; content body, media, review, ChangeSet, hash, logical identity and v0.25.16 UI/IA/schema/visual facts remain unchanged.
- acceptance: pending product/visual review after Engineering local closeout; historical corpus, binding idempotency/drift, Registry CAS, old publication materialization, propagation/recovery and no-duplicate-deployment evidence required.
- contentBoundary: no content transport in this Engineering version; content task may resume the existing revision only after v0.25.17 product acceptance and online finalize.
- commit: recorded by Engineering closeout.
- annotatedTag: `v0.25.17` after final closeout commit.
- clean: required before product/visual acceptance.
- publish: not executed before product/visual acceptance.

## Engineering implementation

- 新增 `scripts/lib/publication-lineage-binding.mjs`：Registry-derived predecessor、registryRevision、bindingHash、原子 sidecar 持久化与 drift 校验。
- SitePublication candidate projection、Coordinator finalize/CAS、public verify 与 completion projection 统一消费 binding；旧 self-supersedes 只保留为不可变历史输入。
- 保留 `ContentSlotRegistry`、legacy migration、`ContentLifecycleAdapter`；补充真实历史 identity、重复 resume、binding drift、Registry CAS 和失败不污染 active 的专项测试。
