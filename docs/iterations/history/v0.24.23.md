# v0.24.23 — 统一媒体独立运营能力与内容发布架构

- 版本：`v0.24.23`
- commit：`d76ff69d2cd32a94c9ba5bd3f74ac4289f0aa438`
- annotatedTag：`v0.24.23`
- clean：`true`（history 提交后、tag/preflight 收口目标）
- parent：`v0.24.22` / `97d095ca5d9c5e6a6cbe92940b188af58f298c80`

## 范围

- 统一 `MediaAsset` 的 `image|video` 类型、MIME/比例/路径/hash/审核/provenance 合同；模块无 `mediaId` 时保留模块结构且不生成媒体占位。
- 扩展 Robotaxi media registry 与字段级 ChangeSet/recovery，保持 source/field identity、beforeHash、来源边界和 canonical 隔离。
- 引入 immutable `baseSiteArtifact`，内容 prepare/build/transport/finalize 使用独立 `contentReleaseId`、manifest、ignored package 与阶段日志，不读取当前产品 HEAD/tag/current/preflight，不修改产品版本事实。
- 产品 publish 合同保持 transport-only；未 push、publish、deploy。

## 不做

- 不修改 UI、IA、schema、路由、页面结构、视觉系统、上游事实或 Practice video 特例。
- 不创建 branch、worktree、task、automation/scheduler；不移动或覆盖 v0.24.22 tag。

## 验收合同与证据

- `npm run check`、`npm run release:prepare`、内容/registry/Practice 专项和 `git diff --check` 通过。
- 全量 `npm run test:sites`：`140` 项中 `139` 通过；唯一失败为既有 Mermaid/Puppeteer macOS 浏览器 I/O（`tests/framework-experience.test.mjs`），未修改该环境问题。
- `npm run release:closeout-check` 在首个 local commit 前通过；annotated tag 与 history 提交后运行 `npm run release:preflight`。
- Local URL：`http://127.0.0.1:4317/`（本次未启动）。Online：`https://xingbuild.top/` 继续线上 v0.24.22，未执行本版本 publish。
