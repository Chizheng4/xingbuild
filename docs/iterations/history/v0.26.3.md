# v0.26.3 — Baseline 2 视觉验收差异收口

父版本：`v0.26.2` / `88e0880b472fe94b082db2c8017ae209b9eab56c`

## 范围

- OA-01：首页主 CTA 为“查看最新B端产品”并进入 `/products`；产品页继续使用已登记 Robotaxi 外链 CTA。
- OA-02：共享 `ActionGroup` 为 Hero action 组提供等宽按钮，保持 390/320px 单行与无溢出。
- OA-03：`ShowcaseModule` 在 `group` 为空或与模块标题相同时隐藏视觉 label，保留字段能力。
- OA-04：当前 `ProductHero` 不渲染 boundary；`ClosingAction` 隐藏默认 eyebrow 与重复摘要，保留结构和入口。
- OA-05：`MediaStage` 使用单层低扩散轻阴影，正文、简讯、About 与 ClosingAction 保持平面。

## 验证合同

- 组件/静态视觉专项覆盖 CTA 语义、等宽 action、重复 label、boundary/eyebrow/summary 条件和单层媒体阴影。
- `npm run check`、`npm run release:prepare`、`npm run release:qa`、`npm run release:closeout-check`、`npm run release:preflight`、`git diff --check` 与 exact HEAD final `release:build` 通过。
- Web `1600×1067`、Mobile `390×844`、窄屏 `320×844` 五路由验证每页一个 H1、无横向溢出、无 console error、键盘焦点和 Reduced Motion 合同。
- 不修改 ContentSet、正文、审核、来源、媒体、ProductArtifact、SiteSnapshot、SitePublication、Coordinator 或既有内容身份；不执行 content publish。

## 状态

Engineering local implementation checkpoint；待产品/视觉按 `xingbuild-visual-ux-review` 独立复验，尚未 product transport。
