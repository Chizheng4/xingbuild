# v0.24.22 — Registry 固定合同与 rollback 基线漂移门禁

- version: `v0.24.22`
- commit: `1fa0b2fd454fbe2efb09639904693847c19634af`（Engineering 本地实现提交）
- annotatedTag: `v0.24.22`
- clean: `true`（本 history 收口提交后）
- parent: `v0.24.21` / `c11025c4b089ce2b3f573794a026901a07305379`

## 范围

- 对 Robotaxi `product-content` registry target 固定 `editable=true`、`scope=field`、`valueType=string`、`content/products/robotaxi.json`、`/products` 与登记字段模式；来源、类型、路径或路由漂移硬停止。
- rollback prepare 在任何 overlay 写入前校验 canonical 当前字段等于原始 `originalBefore` 且 hash 一致；通过后重建原始已发布值，再由逆向 ChangeSet 的 `beforeHash` 完成真实门禁。
- 保留既有 `content:target`、字段级 ChangeSet、staging 与独立内容 prepare/build/transport。

## 不做

- 不修改 v0.24.21 tag；不改 UI、IA、schema、页面结构、路由、组件、CSS、交互、Practice video、媒体合同或上游事实。
- 不扩大内容字段白名单，不新增第二套发布引擎、CMS、RBAC、task、branch、worktree 或 automation。
- 不 push、publish、部署或公网验收。

## 验收合同

- `npm run check`、`npm run release:prepare`、`npm run release:closeout-check`、`npm run release:preflight`、`git diff --check` 通过。
- 内容/发布专项 26/26 通过；registry 篡改、Robotaxi 类型/路径/路由漂移、rollback canonical 漂移与成功恢复专项通过。
- Local URL `http://127.0.0.1:4317/` 未启动；Online 仍为 `https://xingbuild.top/` 的既有线上版本；活动 candidates 无新增。
