# v0.24.34

- version: `v0.24.34`
- implementation commit: `1dbaa30`
- annotatedTag: `v0.24.34` (created after history closeout)
- parent: `v0.24.33` / `43c8166cb52ebc2dfa249bddac219fbcc414deef`
- scope: resumable content CLI, asynchronous deployment evidence, and upload quota preflight
- clean: required and verified by final preflight

## Acceptance

`content-release.mjs --resume --package <dir>` resumes the same package and reports contentReleaseId, sitePublicationId, deploymentId, and publicVerify. Deployment responses may remain pending/processing while deploymentId is persisted for resume; missing machine-readable identity remains a hard failure. File count, maximum single-file size, and total upload size are checked before transport. Active+candidate merge, combined verify, and no-finalize-on-failure remain enforced.

## Verification

- `npm run check`: passed
- `npm run release:prepare`: passed
- content/site publication and quota tests: 10/10 passed
- `npm run release:build`: passed
- `npm run release:closeout-check`: passed before history closeout
- `git diff --check`: passed
- `npm run release:preflight`: run after annotated tag

## Release status

Local only; no push/publish/deploy. Product online remains v0.24.32. Local URL `http://127.0.0.1:4317/` not started.
