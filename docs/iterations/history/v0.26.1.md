# v0.26.1

父版本：`v0.26.0` / `1402304cf0cc6cee3ecaafbb134199c8651366b4`

## 范围

- 建立唯一 `ProductArtifactIdentity` resolver/adapter，三份 ProductArtifact manifest 只在边界读取和校验一次。
- 让 `SiteSnapshot`、`ContentSet`、`PublicationRun`、`SitePublication` 与 Coordinator 只消费扁平身份对象。
- 缺字段、嵌套身份漂移、四元组不一致和重复组装身份漂移在部署前硬失败。
- 复用 v0.26.0 已迁移的 35-entry active ContentSet；不改内容正文、审核、媒体、UI/IA/schema/视觉或旧 tag/history。

## 验证合同

- resolver → snapshot → publication → Coordinator 全链路身份一致；
- 旧 `product-artifact-v1` manifest 仅经适配器兼容读取；
- 同一 ProductArtifact + ContentSet 的 snapshot/publication identity 稳定，resume 不重复 deployment；
- `npm run check`、`npm run release:prepare`、专项测试、`npm run release:closeout-check`、`npm run release:preflight`、`git diff --check` 通过；
- final `release:build` 在本版本 exact HEAD/tag 上生成匹配 ProductArtifact。

## 状态

Engineering local implementation checkpoint；产品/视觉独立验收与产品 transport 尚未开始。
