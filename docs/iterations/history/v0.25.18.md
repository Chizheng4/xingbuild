# v0.25.18 — ContentSlotRegistry 权威边界与一次性 Legacy 迁移

- version: `v0.25.18`
- parent: `v0.25.17 / e1cdc09182e91ca49d6dc2e6353e775837b15caf`
- scope: separate authoritative ContentSlotRegistry reads from explicit legacy bootstrap; once the registry is authoritative, runtime active-slot resolution is corpus-independent.
- compatibility: preserve `PublicationLineageBinding`, `ContentLifecycleAdapter`, SitePublication lease/publicVerify/CAS/finalize, 33 observation active identities and Robotaxi Practice facts; historical package conflicts remain diagnostic evidence only.
- acceptance: real v0.25.17 corpus with authoritative Registry and legacy conflicts runs without rescanning; missing/legacy Registry bootstrap still hard-fails on unresolved predecessor or multiple leaves; schema/migration proof and CAS drift remain hard failures.
- contentBoundary: no content transport in this Engineering version; the content task may later reuse `profile-about-93ea5608339c4973` only after v0.25.18 product acceptance and online finalize.
- commit: recorded by Engineering closeout.
- annotatedTag: `v0.25.18` after final closeout commit.
- clean: required before product/visual acceptance.
- publish: not executed before product/visual acceptance.

## Engineering implementation

- `scripts/lib/content-slot-registry.mjs` now exposes authoritative read and explicit legacy bootstrap paths. `ensureContentSlotRegistry` returns authoritative input without calling `scanLegacyContentSlotRegistry` when `mode=authoritative`.
- Authoritative migration evidence validates schema, mode, source cardinality, self-contained source hash and retained historical conflicts; it never reinterprets the released package corpus.
- Added regression coverage for the real 34-slot corpus, conflicting legacy fixtures, missing/legacy bootstrap hard failure, proof-hash drift, Registry CAS and repeated binding/finalize behavior.
