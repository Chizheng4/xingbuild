# v0.24.17 — Ops 定时采集与内容 task 唯一职责治理

- 状态：Engineering 已完成唯一调度身份与 task 责任治理、自 QA、本地 commit/tag；产品/视觉验收待确认，未 push、publish、部署或公网验收。
- 父版本：`v0.24.16` / `b3e48819839f93db4c01633c62bdbed0720e8193`；父 tag 不修改。
- 范围：唯一 `xingbuild` 调度器登记、Ops owner/运行 task/内容 receiver 身份分离、自动化资源创建门禁、内容 task 调度禁区。
- 不做：不删除或暂停任何既有 task/自动化；不修改 UI、IA、schema、内容、上游事实、产品发布逻辑或内容独立发布身份。
- 验收：唯一活动 scheduler；内容 task 无 scheduler 创建路径；Ops 运行仅写 ignored ops 输出并回传一次；缺失/重复/owner 不明只报告阻断。
