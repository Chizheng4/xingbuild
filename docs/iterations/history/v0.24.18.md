# v0.24.18 — 无状态产品工程迭代闭环

- version: `v0.24.18`
- commit: `2841f5796630c6ecabcba40735112d68b40cf960`（Engineering 实现提交）
- annotatedTag: `v0.24.18`
- clean: `true`（实现提交后工作区已核对；本历史文件为随后一次性收口记录）
- parent: `v0.24.17` / `647e2137ae56f9837cf3e40ee711431a926a392a`

## 范围

- 移除 current/history 生命周期状态字段门禁；current 只保存当前可执行产品方案。
- closeout、preflight 与 product publish 从 package、VERSION、current 版本号、Git HEAD/tag 和 clean 工作区推导本地事实。
- 删除旧 `version-state` 生命周期状态模块，补齐无状态字段专项测试。

## 不做

- 不修改 v0.24.17 或更早版本的 tag/history。
- 不修改 UI、IA、schema、内容、上游事实或独立内容发布合同。
- 不 push、publish、部署，不写入产品/视觉验收、publish 授权或线上状态。

## 验收合同

- `current.md` 不含 `localSubmission`、`productVisualAcceptance`、`publishAuthorization`、`onlineRelease` 等生命周期字段。
- 版本号、HEAD、annotated tag 与 clean 工作区不一致时，closeout/preflight 必须阻断。
- Engineering 本地版本收口后交产品/视觉验收；后续验收与发布事件只写外部 QA、manifest、部署或公网验证证据。
