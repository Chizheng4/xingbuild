# v0.2.1 一键发布确认逻辑

## 问题

双击 `publish-xingbuild.command` 后，脚本再次要求输入 `publish`；如果未输入，GitHub 与 EdgeOne 都不会更新。

## 完成结果

- 将双击发布命令本身固定为明确的生产发布动作。
- 移除脚本内部二次文本确认。
- 保持发布前检查、GitHub 同步、EdgeOne 生产部署和公网版本验证的完整顺序。

## 边界

- 本版本只修正发布流程，不自动触发生产发布。
