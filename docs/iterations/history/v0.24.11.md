# v0.24.11 发布构建只读消费已提交生成物

## 结果

- 状态：Engineering 已完成构建纯度根修正、自 QA、本地 commit/tag；产品/视觉验收、publish 授权、部署和公网验证均为提交后外部事件。
- 范围：release build 不调用 `architecture:views`、`framework:data`、`framework:layout`、`article:figures`；这些命令保留为 local commit 前显式生成入口。构建后的 tracked dirty 门禁继续硬失败。
- v0.24.10 tag/commit 保留不动；不修改 UI、IA、schema、内容或上游事实。

## 验证

- `npm run check`、相关生成/架构测试、`npm run release:check`、`npm run release:closeout-check`、`npm run release:preflight`、`git diff --check` 按本地收口记录。
- 本地 URL：`http://127.0.0.1:4317/`（未启动）；线上继续 v0.24.1：`https://xingbuild.top/`。
