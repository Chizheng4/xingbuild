# v0.26.2 — 全站视觉 Baseline 1→2 差异与组件契约

父版本：`v0.26.1` / `e0b1fae8a738a6d442cbd1235b431b530824ca8a`

## 范围

- 复用既有 `PageComposition`、`ShowcaseModule`、`ObservationRail`、`RichDocument`、`ClosingAction` 与 shared tokens，完成 Baseline 1→2 已确认的首页、产品、经营观察、About 和全局辅助色差异。
- 首页顺序收敛为最新作品、Robotaxi 运营平台、ClosingAction、最新观察简讯与更多观察；经营观察明确页面 H1 与右侧最新简讯的标题责任。
- 产品 Hero 和 ShowcaseModule 的可选眉题、边界说明与 label 采用条件渲染收紧；手机端说明在媒体上方，ClosingAction 使用等宽纵向动作。
- 阅读内容与观察集合保持近乎平面的冷白表面；媒体窗口继续独占轻媒体阴影，辅助文字统一使用 `#64748B`。
- 不修改 ContentSet、正文、审核、来源、媒体、ProductArtifact、SiteSnapshot、SitePublication、Coordinator 或既有页面事实。

## 验证合同

- v0.26.2 静态视觉/页面组合专项通过；既有页面组合、媒体、焦点、Reduced Motion 和响应式合同保持。
- `npm run check`、`npm run release:prepare`、`npm run release:closeout-check`、`npm run release:preflight`、`git diff --check` 与 exact HEAD final `release:build` 通过。
- `npm run release:qa` 中既有内容 fixture 漂移与 macOS Chromium sandbox I/O 作为 retained/environment incident 分层保留，不隐藏、不归因于本版本视觉差异。
- 产品/视觉独立验收及 product transport 不属于本地 Engineering commit；本版本不执行 content publish。

## 状态

Engineering local implementation checkpoint；待产品/视觉以最终 ProductArtifact 独立验收，尚未 transport 或恢复内容运营。
