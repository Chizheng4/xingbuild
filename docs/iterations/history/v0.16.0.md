# v0.16.0 企业经营体系可读架构图运行时升级

## 完成范围

- 使用 `@xyflow/react` 建立只读架构图运行时，保持总览 → 数字化实现 → 节点聚焦的唯一阅读路径。
- 使用 `elkjs` 在构建阶段生成 desktop/mobile 确定性布局，ELK 不进入浏览器运行时。
- 保持 `frameworkModel` 的概念、定义、作用、9 个节点、13 条边、方向和关系文案不变。
- 手机完整架构参与页面自然滚动，不建立内外双重纵向滚动。

## 验证与收口

- 专项测试与 `npm run release:check` 通过。
- 1440、1024、768、557、390、320 六档真实页面验证通过；无横向溢出、嵌套纵滚或 console error/warning。
- 最终产品提交与 tag：`f62ac491b5ee96ca0a2976eb6c90744f4560ad94` / `v0.16.0`。
- GitHub main/tag、EdgeOne 生产部署和公网 release/manifest 验收已完成；后续日常内容提交保持产品 tag 为 `v0.16.0`。

## 后续结论

生产使用暴露的默认关系显隐、画布稳定性、局部工具栏和全站返回一致性问题不回写本历史，进入独立 `v0.17.0`。
