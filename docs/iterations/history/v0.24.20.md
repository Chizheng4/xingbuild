# v0.24.20 — 声明式 B 端内容定位与字段级 ChangeSet

- version: `v0.24.20`
- commit: `1638b8d`（Engineering 本地实现提交）
- annotatedTag: `v0.24.20`
- clean: `true`（本次 history 提交完成后）
- parent: `v0.24.19` / `1d93c6f3124ce9fb53b9e61c577d6a6ffd208832`

## 范围

- 消费 `content/registry/content-targets.json`，仅支持已登记的 Robotaxi 产品与模块字段定位。
- 生成被忽略的 `.content-workspace/changes/` 结构化 ChangeSet，校验 targetId、fieldPath、beforeHash、after、affectedRoutes、sourceRefs、boundary 与 authority。
- 复用独立内容 `prepare → build → transport`，在 staging copy 叠加字段变更；canonical 工作区不写入 tracked 内容。

## 不做

- 不改 UI、IA、schema、路由、组件、CSS、交互、上游事实或产品版本发布逻辑。
- 不接受未登记目标、整文件覆盖、数组顺序/文本猜测；不新增 CLI、CMS、RBAC、外部数据层或 Practice 媒体合同。
- 不 push、publish、deploy；不移动或修改既有 `v0.24.19` tag。

## 验收合同

- `npm run check`、`npm run release:prepare`、`npm run release:closeout-check`、`git diff --check` 通过。
- 内容目标解析、ChangeSet 范围/hash 冲突、§6.1 能力建设/能力使用边界、staging overlay 与 canonical clean 专项测试通过。
- 产品 publish 继续只消费既有产品版本身份；本版本未执行线上发布，Local URL `http://127.0.0.1:4317/` 未启动，Online 仍为 `https://xingbuild.top/` 上的既有线上版本。
