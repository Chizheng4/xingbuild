# v0.24.9 版本状态机与收口事实治理

## 结果

- 状态：Engineering 已完成状态字段、事实一致性门禁、自 QA、本地 commit/tag；产品/视觉验收待确认，尚未 push、publish、部署或公网验收。
- 范围：current 统一记录 `localSubmission`、`productVisualAcceptance`、`publishAuthorization`、`onlineRelease`；closeout/preflight 与状态测试阻止自然语言、Git/tag、线上事实不一致。
- 修正 v0.24.8 状态事实；不回写或移动 v0.24.8/v0.24.7 tag，不修改 UI、IA、schema、内容或上游事实。

## 验证

- `npm run check`、状态/发布专项测试、`git diff --check`、`npm run release:closeout-check`、`npm run release:preflight` 通过。
- 本地 URL：`http://127.0.0.1:4317/`（未启动）；线上继续 v0.24.1：`https://xingbuild.top/`。
