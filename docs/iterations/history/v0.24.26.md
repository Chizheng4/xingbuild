# v0.24.26 — 空内容安全与完整内容目标能力

- 版本：`v0.24.26`
- commit：`3d51a36a93589407952ced2316346db9502d1605`（Engineering 本地实现提交）
- annotatedTag：`v0.24.26`（history 收口后创建）
- clean：`true`（history 收口、tag 与 preflight 后）
- parent：`v0.24.25` / `c83900f7b06106af9392c05359cac4a70fc2a2fd`

## 范围

- page content resolver 对缺失 profile、practice、businessObservation、article 与 observationBriefs 安全返回 `null`/空集合；修复 `/about` 在 profile 缺失时访问 `profile.id` 的运行时错误，并用路由级空内容专项锁定 `/`、`/products`、`/business-observations`、`/observations`、`/about` 的合法空内容模型。
- 独立 `content-release` 支持 `profile` 与 `businessObservation`，与既有 `content`、`article`、`practice` 共用 prepare/build/transport/finalize；每个包继续记录 `contentReleaseId`、`contentHash`、`baseSiteArtifactId`、`deploymentId`、`publicVerify`，并在 manifest 中记录精确目标身份。
- 产品 build 继续只读产品能力与稳定产品源，不读取或复制 ignored `.content-workspace/content`；产品 manifest 不写入独立内容发布身份。

## 不做

- 不修改 UI、IA、schema、路由、上游事实、v0.24.25 tag/history 或内容 task；不创建 branch、worktree、task、automation；不 push、publish、deploy。

## 验收合同与证据

- `npm run check`、`npm run release:prepare`、`git diff --check`、提交前 `npm run release:closeout-check` 通过。
- 空内容路由、profile/businessObservation 内容目标、内容隔离、ChangeSet/registry 与既有 Practice/发布专项通过；全量 `node --test tests/*.test.mjs` 为 146 项中 145 项通过。
- 全量唯一失败为既有 Mermaid/Puppeteer macOS 浏览器 I/O（`tests/framework-experience.test.mjs`），未修改或放宽该环境门禁。
- `npm run build` 实测产品构建未读取独立内容根与媒体；内容目标 staging build 使用既有 immutable baseSiteArtifact，canonical 工作区保持无 tracked 脏改。
- Local URL：`http://127.0.0.1:4317/`（本次未启动）。Online：`https://xingbuild.top/` 仍为 v0.24.24，未执行本版本 publish。
- 活动候选无新增；产品/视觉验收与用户 publish 授权为下一动作。
