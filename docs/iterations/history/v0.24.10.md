# v0.24.10 不可变版本身份与提交后事件分离

## 结果

- 状态：Engineering 已完成本地 commit/tag 与自 QA；产品/视觉验收、publish 授权、部署和公网验证均为提交后外部事件，未写入本文件。
- 范围：current/history 只保存不可变 localSubmission 与版本身份事实；publish 只消费 clean HEAD+annotated tag，并以显式参数或环境变量作为授权门禁。
- v0.24.9、v0.24.8、v0.24.7 tag 均保留不动；不修改 UI、IA、schema、内容或上游事实。

## 验证

- `npm run check`、release/状态专项测试、`git diff --check`、`npm run release:closeout-check`、`npm run release:preflight` 通过。
- 本地 URL：`http://127.0.0.1:4317/`（未启动）；线上继续 v0.24.1：`https://xingbuild.top/`。
