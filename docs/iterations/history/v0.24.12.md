# v0.24.12 publish 纯传输部署与预生成产物合同

## 结果

- 状态：Engineering 已完成 publish transport 拆分、自 QA、本地 commit/tag；业务验证和构建由 publish 前的 `release:prepare`/`release:build` 承担。
- 范围：publish 只校验 clean HEAD+annotated tag 与 `dist/client/release.json`、`content-manifest.json`，再执行授权后的 push、EdgeOne deploy 和公网 manifest 验证；不运行 release:check、build、Sites、生成器或内容业务 QA。
- v0.24.11 tag/commit 保留不动；不修改 UI、IA、schema、内容或上游事实。

## 验证

- `npm run check`、transport/状态专项测试、`npm run release:closeout-check`、`npm run release:preflight`、`git diff --check` 记录于本地收口；`release:check` 记录实际结果。
- 本地 URL：`http://127.0.0.1:4317/`（未启动）；线上继续 v0.24.1：`https://xingbuild.top/`。
