# v0.25.16 — 内容活动槽位注册表与原子替换发布

- version: `v0.25.16`
- parent: `v0.25.15 / 084148068860dad6ddb7288fed51b15f618521bd`
- scope: introduce one versioned `ContentSlotRegistry` across content kinds; migrate the historical finalized receipt/package corpus; resolve the real Practice predecessor; finalize replacement snapshots with predecessor compare-and-swap.
- acceptance: pending independent product/visual review after Engineering local closeout; historical corpus, legacy replacement, multi-operation ChangeSet, repeated resume, stale lease, projection drift, active concurrency, ProductArtifact compatibility and failure preservation evidence required.
- contentBoundary: preserve the existing `practice-robotaxi-604214b3bfddf09f` package, `revision-9bb22df0f30845e8`, ChangeSet, contentHash, media, review and 34 active identities; no content transport in this Engineering version.
- commit: recorded by Engineering closeout.
- annotatedTag: `v0.25.16` after final closeout commit.
- clean: required before product/visual acceptance.
- publish: not executed before product/visual acceptance.

## Engineering implementation

- 新增 `scripts/lib/content-slot-registry.mjs`：版本化 schema/repository、legacy migration、唯一 active resolver、initial registration、compare-and-swap、restore 与冲突报告。
- SitePublication active set 与 receipt projection 只读取 registry；ProductArtifact 生成并校验 `content-slot-registry-v1` capability contract，legacy corpus 仅在迁移模式兼容旧 artifact。
- replacement candidate 的 predecessor 由 registry 解析，finalize 在精确 publicVerify 后原子推进 active；同一 publication 的重复 finalize/resume 复用既有 transition，不生成第二 active 或第二 deployment。
- 保留 v0.25.15 kind-specific lifecycle adapter；补充真实历史 corpus、冲突 migration、CAS/self/stale、artifact capability 与 SitePublication finalize 幂等测试。
