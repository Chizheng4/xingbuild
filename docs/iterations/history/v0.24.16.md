# v0.24.16 — 内容运营独立发布治理

- 状态：Engineering 完成内容独立 prepare/build/transport/finalize 实现、自 QA 与版本记录；本地 commit/tag 后交产品与视觉验收。
- 范围：内容发布包写入 ignored `.content-workspace/releases/`，在临时 staging copy 中构建；内容 publish 不创建/递增产品版本，不修改 `current.md`、`VERSION.md`、产品 history，不 commit/tag/push。
- 产品 publish：`unified-publish --kind product` 继续只消费 clean main 的既有版本身份与预生成产品 `release.json`。
- 不做：不修改 UI、IA、schema、外部数据层、上游事实，不移动或修改 v0.24.15 tag，不 publish/deploy。
- 验收：`npm run check`、内容/产品发布专项测试、`git diff --check`、`release:prepare`、`release:build`、`release:closeout-check`、`release:preflight`；既有 Mermaid/Puppeteer I/O 仅记录为环境阻断。
