# v0.26.4 — H-02 首页产品内容区标签位置

父版本：`v0.26.3` / `211b3c5c4cfcdf1c79ac4799c7e10111f537a718`

## 范围

- 将首页 `最新作品` 从 `ProductHero` 的居中 `heroEyebrow` 移入共享 `PracticePresentation` 的产品内容区结构。
- 产品区标签与 `Robotaxi运营平台` 标题共享左起点，并使用 `--space-1`（4px）紧密绑定；首页 Hero 保留个人定位、说明和两个 CTA。
- 复用既有 `PageComposition`、`ShowcaseFlow`、`ProductHero` 与 tokens；不修改 ContentSet、正文、审核、来源、媒体或 SitePublication 架构。

## 验证合同

- Web `1600×1067`、Mobile `390×844`、窄屏 `320×844`：H-02 左对齐、4px 间距、无空占位、无横向溢出。
- 五路由每页一个 H1、无 console/page error；既有 OA-01～OA-05、键盘焦点、Reduced Motion 与内容 fallback 不回归。
- `npm run check`、`release:prepare`、分层 QA、`release:closeout-check`、`release:preflight`、`git diff --check` 与 exact HEAD final build。

## 状态

Engineering local implementation checkpoint；待产品/视觉独立验收，尚未 product transport。
