# v0.15.7 — Slug 级内容审核终端聚合

## 目标

为已完成人工事实审核和公开表达判断的单个 Observation 提供显式 `slug + authority` 终端入口，在共享 JS 能力层聚合既有 review 与 promote。

## 完成范围

- 新增 `npm run content:approve -- --slug <slug> --authority <authority>`，严格拒绝缺失、空值、多 slug、非法参数与目标生命周期冲突。
- 成功仅新增目标 review、recovery 与 production 并保留 draft；失败精确回滚本次新增文件。
- 保持独立 `content:promote` 的兼容语义：production 写入失败时保留已生成 recovery，draft/review 不变。
- 无关 ignored workspace 不扫描、不阻断、不修改；命令不承担选题、写稿、提交、推送或发布。

## 验证与发布

- 内容专项 29/29、完整 `npm run release:check` 69/69、生产构建 14 条 Observation 通过。
- 产品独立验收通过；commit/tag 为 `01cd98376cddab3965d117c65165462a5e40b2b9` / `v0.15.7`。
- GitHub main/tag、EdgeOne deployment `dp7bxdoc6exe` 与公网 release/manifest 同 SHA 验收完成。
- 版本发布后产生的 Observation 内容提交与统一问题治理提交保持产品版本/tag 不变，不属于本产品版本范围。

## 归档状态

v0.15.7 已完成实现、验证、专业验收、本地收口与公网发布；后续迭代不得回写本历史。
