# v0.24.8 统一发布消费现有版本与构建纯度治理

## 结果

- 状态：Engineering 实现与自 QA、本地 commit/tag 已完成；产品/视觉验收、push、publish、部署和公网验收尚未完成。
- 范围：发布器只消费已验收的现有 local commit/tag；移除自动版本递增、版本记录回写、commit/tag 和失败完成声明；构建使用精确 HEAD 内部沙箱并对 tracked dirtyPaths 硬失败。
- 不修改 UI、IA、schema、内容事实或上游事实；保留异常 v0.24.7 tag/commit，不 push、deploy 或 publish。

## 验证

- 专项 unified publish、Practice、content workflow 测试与 `node --check scripts/unified-publish.mjs` 通过。
- `npm run check`、`git diff --check`、`npm run release:closeout-check`、`npm run release:preflight` 在本地收口时记录。
- 本地 URL：`http://127.0.0.1:4317/`（未启动）；线上继续 `v0.24.1`：`https://xingbuild.top/`。
