# v0.25.15 — 内容类型生命周期适配与 Package 证明式 Reconcile

- version: `v0.25.15`
- parent: `v0.25.14 / b0bb49dcebb0b5b888ac844115f59695be0cf4cb`
- scope: route prepare/reconcile/finalize through one kind-specific `ContentLifecycleAdapter`; prove immutable Practice canonical before + ChangeSet operations → package after/contentHash, with review/media and recovery envelopes.
- acceptance: pending independent product/visual review after Engineering local closeout; Practice before/after, wrong evidence, replacement, finalize, failure preservation and resume evidence required.
- contentBoundary: preserve the existing `practice-robotaxi-604214b3bfddf09f` package, ChangeSet, contentHash, media, review and 34 active identities; no content transport in this Engineering version.
- commit: recorded by Engineering closeout.
- annotatedTag: `v0.25.15` after final closeout commit.
- clean: required before product/visual acceptance.
- publish: not executed before product/visual acceptance.

## Engineering implementation

- 新增 `scripts/lib/content-lifecycle-adapter.mjs` registry，统一暴露 canonical、review、recovery、before/after validation 与 finalize 路由。
- Practice reconcile 接受 canonical before，读取 immutable package after 与四项 ChangeSet operations，验证 `604214b3bfddf09f...` contentHash、approved media provenance、logical identity 和 ProductArtifact 后生成可幂等复用的 revision wrapper。
- package / receipt / completion / SitePublication receipt projection 保留 before/after hash、ChangeSet、review/recovery 证明；旧 Observation 生命周期文件继续兼容。
- public verify 成功后 Practice canonical 才原子推进 after，并把 before snapshot 写入 package recovery；重复 finalize 验证 after 并幂等返回。
