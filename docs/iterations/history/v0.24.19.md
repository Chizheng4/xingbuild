# v0.24.19 — 产品工程协作治理基线结构化

- version: `v0.24.19`
- commit: `6dc854174d5cc7c0d7301732aa2989a09e8313aa`（Engineering 实现提交）
- annotatedTag: `v0.24.19`
- clean: `true`（实现提交和本历史记录提交后工作区已核对）
- parent: `v0.24.18` / `ba1be3d9e12c6b285ff031dd24b00670584f9a41`

## 范围

- 新增 `00-baseline-index.md`，统一规则优先级、五层结构、文件 owner 和按任务类型读取矩阵。
- 新增职责与内部工作流、跨 task 协作、Engineering 架构与原则三份唯一正文。
- `AGENTS.md` 收敛为入口与强制边界；`iteration-and-release.md` 收敛为产品版本、验证、Git、EdgeOne、回退和 Publish Incident 规则。
- 明确 `sourceThreadId`、`targetThreadId`、`returnThreadId` 分离，长任务 ACK、一次性交接、一次回传和禁止猜测/轮询/后台等待。
- 保留现有产品总案、内容运营合同和经营观察合同为各自唯一事实源；内容与 Ops 身份不进入产品版本闭环。
- 增加五层入口、职责分离、协作身份和运营独立边界专项测试。

## 不做

- 不修改 v0.24.18 或更早版本的 tag/history。
- 不修改 UI、IA、schema、内容、上游事实、产品总案正文、内容运营合同或 publish 业务逻辑。
- 不创建并行 task、branch、worktree、automation 或 scheduler；未 push、publish、部署或公网验收。

## 验证

- `git diff --check`、`npm run check`、`npm run release:prepare`、`npm run release:closeout-check`、`npm run release:preflight` 通过。
- 规则专项 8/8 通过；完整 `test:sites` 为 131 项，130 通过，唯一失败为既有 Mermaid/Puppeteer macOS 浏览器 I/O（`sandbox_parameters_mac.mm:87`）。
- 官方 `main` clean；Local `http://127.0.0.1:4317/` 未启动；Online 仍为 v0.24.13 `https://xingbuild.top/`；未 push/publish/deploy。
