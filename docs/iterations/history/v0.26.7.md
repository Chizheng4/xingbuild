# v0.26.7 — 首页 CTA 共享尺寸回归修复

父版本：`v0.26.6` / `098499040449aadd8f4bdc14a11bd9fd9df10889`

## 范围

- Home 使用独立对齐容器负责 Hero 主轴居中；共享 `ActionGroup equalWidth` 保持 `--measure-action-group` 最大宽度，桌面按钮约 218px 等宽。
- Mobile `390×844`、`320×844` 随内容容器收缩，按钮单行且无溢出；Products 尺寸、CTA、视频、H-02 和经营观察已通过项保持。
- 不修改 ContentSet、ContentReleaseId、正文、审核、来源、media、content CLI、Coordinator 或 SitePublication。

## 验证

- 五路由 Web/Mobile viewport-only 证据：`1600×1067`、`1280×1067`、`390×844`、`320×844`；Home/Products geometry、CTA href、视频属性、键盘/Reduced Motion 回归。
- `npm run check`、`release:prepare`、视觉专项、closeout、preflight、exact final build。

## 状态

待产品/视觉独立验收，尚未 product transport。
