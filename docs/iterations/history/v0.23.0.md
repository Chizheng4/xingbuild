# v0.23.0 统一能力展示控件与可组合表达

## 结果

- 状态：已完成实现、产品/视觉验收、收口、推送、部署与公网验收。
- commit/tag：`3d2f90c39ac5952aa122d1d4ee6aacd117b65e28` / `v0.23.0`。
- EdgeOne deployment：`dp9oxdxvo4a4`。
- 公网：`https://xingbuild.top/`。

## 交付范围

- 新增声明式 `CapabilityStage / VisualizationHost` 能力展示主机。
- 统一图片、视频、图形和受控互动空间的入口、状态、键盘/触控语义、响应式容器与文本降级。
- 使用既有文章图形和 `Showcase/SystemStage` 作为兼容性 fixture，不固定运营内容。
- 保留现有信息架构、暖色视觉、`RichDocument`、Observation rail、Header/Footer 和独立内容发布合同。

## 验收

- `release:check`：108/108。
- 六档本地响应式、公开首页/`/products`/`/business-observations` 回归通过。
- fixture 的点击、Enter、Space 状态交互通过；无横向溢出与控制台错误。

## 未包含

- 不固定文章或 Robotaxi 模块的运营内容。
- 不修改 Robotaxi 上游、登录态、系统状态或 `/embed` 边界。
- 不改变 Observation/Practice schema、主题、一级导航或内容发布合同。

## 后续

当前没有下一项已确认产品候选；产品版本流水线停止在 v0.23.0 发布基线。内容与运营任务继续独立运行。新的产品优化必须先进入 `docs/iterations/candidates/` 并取得明确执行授权。
