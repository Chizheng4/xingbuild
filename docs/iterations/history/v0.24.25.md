# v0.24.25 — 产品与独立内容发布边界解耦

- 版本：`v0.24.25`
- commit：`1828a0d`（Engineering 本地实现提交）
- annotatedTag：`v0.24.25`（history 收口后创建）
- clean：`true`（history 收口、tag 与 preflight 后）
- parent：`v0.24.24` / `0ea7056ef66a4d088cf224465e07882cce9460b0`

## 范围

- 产品构建以 `XINGBUILD_CONTENT_BUILD` 显式区分产品模式与内容 staging 模式；产品 build 不读取或复制 ignored `.content-workspace/content`、独立运营媒体或内容账本。
- 产品 repository 在产品模式下返回合法空内容状态；页面无内容时不生成占位内容、不改变 UI、IA、schema、路由或视觉系统。
- 产品 `dist/client` 的 `content-manifest.json` 保留产品版本/commit 与空 publication 列表，不承载 `contentReleaseId`、deployment 或 `publicVerify`；产品 `release.json` 只记录产品身份。
- 内容 prepare/build 继续在 immutable `baseSiteArtifact` staging 中显式叠加独立内容，复用既有 `contentReleaseId`、hash、deployment、publicVerify、ChangeSet/recovery 与独立 transport。

## 不做

- 不修改 UI、IA、schema、路由、上游事实、v0.24.24 tag/history 或独立内容 task；不创建 branch、worktree、task、automation；不 push、publish、deploy。

## 验收合同与证据

- `npm run check`、`npm run release:prepare`、产品隔离专项、内容 publish/registry/Practice 专项通过。
- `npm run test:sites`：143 项中 142 项通过；唯一失败为既有 Mermaid/Puppeteer macOS 浏览器 I/O（`tests/framework-experience.test.mjs`），未修改该环境问题。
- 产品 `npm run build` 实测在 canonical 工作区存在独立内容时不携带正文/媒体；产品 manifest publication 列表为空且无内容发布身份字段。
- 内容 staging build 实测继续生成独立内容 manifest，既有 content publish 专项通过。
- `npm run release:closeout-check` 在 history 收口前通过；history 收口后创建 annotated `v0.24.25` 并运行 `npm run release:preflight`。
- Local URL：`http://127.0.0.1:4317/`（本次未启动）。Online：`https://xingbuild.top/` 已有 v0.24.24，未执行本版本 publish。
- 活动候选保留 `XBUILD-PRODUCT-CONTENT-ISOLATION-001` pending；产品/视觉验收与用户 publish 授权为下一动作。
