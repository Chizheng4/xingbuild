# v0.25.2 — 内容发布包身份重建与幂等恢复能力

- version: `v0.25.2`
- parent: `v0.25.1 / f6c5a61c46e3d2860a2c6558a087008c1c8849bb`
- scope: reconcile one logical `ContentReleaseIntent` into an immutable `ContentPackageRevision`; validate lifecycle hashes, approved review, target and base artifact; preserve lineage/recovery; bind batch, SitePublication, resume and active selection to revision identity.
- acceptance: `npm run check`, `npm run release:prepare`, `npm run release:build`, targeted reconcile/batch/state/publication tests, `git diff --check`, closeout, clean local commit and annotated tag.
- nhtsaEvidence: logical `contentReleaseId=content-nhtsa-first-responder-requirement-cdbce9f16feccd94`; prepared `packageRevisionId=revision-8cad6ee1cec96cf3`; repeated reconcile reused the same revision and plan without deployment.
- commit: recorded by Engineering closeout.
- annotatedTag: `v0.25.2` after final closeout commit.
- clean: required before product/visual acceptance.
- publish: not executed; real public recovery remains a post-product-acceptance content operation.
