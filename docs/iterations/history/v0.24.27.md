# v0.24.27

- version: `v0.24.27`
- commit: `1b4340c24a91d8f7612fcbb52862801f38571369`
- annotatedTag: `v0.24.27` (created after this history closeout commit)
- parent: `v0.24.26` / `70847cdf6df0820458c32e2f6df19f6aea7711e8`
- clean: required after tag and preflight

## Scope

Implemented the independent content-release state machine: immutable identity-bound idempotency key, package lease and resume, bounded public verification, atomic finalize facts, preserved recovery evidence, and isolated serial target failures. Product UI/IA/schema/routes, content facts, and product publish boundaries were not changed.

## Acceptance contract

Content releases advance through `prepared → built → transported → verifying → finalized → released`; failures are represented as `failed`, `recoverable`, or `rolled-back`. A retry reuses the same `contentReleaseId`, `contentHash`, `baseSiteArtifactId`, package and deployment. Finalize reads the ignored independent content root and atomically records completion without deleting draft/review/recovery/package/log evidence.

## Verification

- `npm run check`: passed
- `npm run release:prepare`: passed
- content state/publish/workflow tests: 47/47 passed
- full test suite: existing Mermaid/Puppeteer macOS browser I/O failure only; no v0.24.27 failure identified
- `git diff --check`: passed before closeout
- `release:closeout-check`: passed before history closeout
- `release:preflight`: run after annotated tag

## Release status

Local only; no push, publish, or deploy. Local URL: `http://127.0.0.1:4317/` (not started). Online remains `https://xingbuild.top/` on v0.24.26 until explicit product/visual acceptance and user authorization.
